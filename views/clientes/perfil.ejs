<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>

    <body>
        <div class="container-fluid">
            <div class="row flex-nowrap">
                <%- include('../partials/sidebar') %>

                    <div class="col py-3">
                        <div class="container-fluid px-4 py-4">

                            <div class="card card-box mb-4 border-primary border-top border-4">
                                <div class="card-body d-flex align-items-center flex-wrap gap-3">

                                    <div class="me-2">
                                        <% if (cliente.foto) { %>
                                            <img src="/uploads/<%= cliente.foto %>"
                                                class="rounded-circle border border-3 border-white shadow-sm"
                                                style="width: 100px; height: 100px; object-fit: cover;">
                                            <% } else { %>
                                                <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center shadow-sm"
                                                    style="width: 100px; height: 100px; font-size: 2.5rem;">
                                                    <%= cliente.nombre.charAt(0) %>
                                                </div>
                                                <% } %>
                                    </div>

                                    <div class="flex-grow-1">
                                        <h2 class="mb-0 fw-bold">
                                            <%= cliente.nombre %>
                                                <%= cliente.apellido %>
                                        </h2>
                                        <p class="text-muted mb-2">CC: <strong>
                                                <%= cliente.dni %>
                                            </strong></p>

                                        <div class="d-flex gap-2 flex-wrap">
                                            <span class="badge bg-light text-dark border"><i
                                                    class="bi bi-geo-alt-fill text-danger"></i>
                                                <%= cliente.direccion || 'Sin dirección' %>
                                            </span>
                                            <span class="badge bg-light text-dark border"><i
                                                    class="bi bi-telephone-fill text-success"></i>
                                                <%= cliente.telefono || 'Sin teléfono' %>
                                            </span>
                                            <span class="badge bg-light text-dark border"><i
                                                    class="bi bi-envelope-fill text-primary"></i>
                                                <%= cliente.email || 'Sin email' %>
                                            </span>
                                            <span class="badge bg-light text-dark border" title="Última vez que el cliente ingresó a su portal">
                                                <i class="bi bi-person-badge-fill text-info"></i>
                                                Último acceso: <%= cliente.ultimo_login ? new Date(cliente.ultimo_login).toLocaleString() : 'Nunca' %>
                                            </span>
                                            <% if (cliente.app_instalada) { %>
                                                <span class="badge bg-primary text-white border border-primary shadow-sm" title="El cliente ha instalado la aplicación">
                                                    <i class="bi bi-phone-fill text-white"></i> APP INSTALADA
                                                </span>
                                            <% } %>
                                            <% if (cliente.monto_preaprobado > 0) { %>
                                                <span class="badge bg-warning text-dark border border-warning shadow-sm" title="Cupo disponible para nuevos créditos">
                                                    <i class="bi bi-star-fill text-primary"></i> PRE-APROBADO: <%= empresa.moneda %> <%= formatCurrency(cliente.monto_preaprobado, 0) %>
                                                </span>
                                            <% } %>
                                        </div>
                                    </div>

                                    <div class="text-end mt-3 mt-md-0 d-flex gap-2">
                                        <% if (cliente.monto_preaprobado > 0 && cliente.email) { %>
                                            <button onclick="enviarCorreoPreaprobado(<%= cliente.id %>)" 
                                                    class="btn btn-primary shadow-sm px-4 fw-bold rounded-pill"
                                                    id="btnSendPre">
                                                <i class="bi bi-envelope-check-fill me-2"></i> Notificar Pre-aprobado
                                            </button>
                                        <% } %>
                                        <button onclick="iniciarChat(<%= cliente.id %>)" class="btn btn-success shadow-sm px-4 fw-bold rounded-pill">
                                            <i class="bi bi-chat-dots-fill me-2"></i> Mensaje Directo
                                        </button>
                                        <a href="/reportes/estado-cuenta/<%= cliente.id %>" target="_blank"
                                            class="btn btn-dark shadow-sm px-4 fw-bold rounded-pill">
                                            <i class="bi bi-file-earmark-bar-graph-fill me-2"></i> Estado de Cuenta
                                        </a>
                                        <a href="/clientes" class="btn btn-outline-secondary px-3 rounded-pill" title="Volver al listado">
                                            <i class="bi bi-arrow-left"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>                            <ul class="nav nav-tabs mb-3" id="clientTabs" role="tablist">
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link active" id="prestamos-tab" data-bs-toggle="tab"
                                        data-bs-target="#prestamos" type="button">
                                        <i class="bi bi-cash-stack"></i> Préstamos (<%= prestamos.length %>)
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="empenos-tab" data-bs-toggle="tab"
                                        data-bs-target="#empenos" type="button">
                                        <i class="bi bi-gem"></i> Empeños (<%= empenos.length %>)
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="ahorros-tab" data-bs-toggle="tab"
                                        data-bs-target="#ahorros" type="button">
                                        <i class="bi bi-piggy-bank"></i> Ahorros
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="preaprobados-tab" data-bs-toggle="tab"
                                        data-bs-target="#preaprobados" type="button">
                                        <i class="bi bi-star-fill text-warning"></i> Pre-aprobados
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="documentos-tab" data-bs-toggle="tab"
                                        data-bs-target="#documentos" type="button">
                                        <i class="bi bi-file-earmark-pdf-fill"></i> Documentos (<%= documentos.length %>)
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link" id="scoring-tab" data-bs-toggle="tab"
                                        data-bs-target="#scoring" type="button">
                                        <i class="bi bi-shield-check-fill text-success"></i> Score Crediticio
                                    </button>
                                </li>
                            </ul>

                            <div class="tab-content" id="clientTabsContent">
                                <!-- Tab Préstamos -->
                                <div class="tab-pane fade show active" id="prestamos" role="tabpanel">
                                    <div class="card card-box p-0 border-0 shadow-sm overflow-hidden">
                                        <% if(prestamos.length > 0) { %>
                                            <div class="table-responsive">
                                                <table class="table table-hover align-middle mb-0">
                                                    <thead class="table-light text-uppercase small fw-bold">
                                                        <tr>
                                                            <th>ID</th>
                                                            <th>Fecha</th>
                                                            <th>Prestado</th>
                                                            <th>Pagado</th>
                                                            <th>Saldo</th>
                                                            <th>Estado</th>
                                                            <th class="text-end pe-3">Acción</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        <% prestamos.forEach(p => { 
                                                            const pagado = p.total_pagado || 0;
                                                            const saldo = p.monto_total - pagado;
                                                        %>
                                                        <tr>
                                                            <td><span class="badge bg-dark">#<%= p.id %></span></td>
                                                            <td><%= new Date(p.fecha_inicio).toLocaleDateString() %></td>
                                                            <td class="small fw-bold"><%= empresa.moneda %> <%= formatCurrency(p.monto_prestado, 2) %></td>
                                                            <td class="text-primary fw-bold"><%= empresa.moneda %> <%= formatCurrency(pagado, 2) %></td>
                                                            <td class="text-danger fw-bold"><%= empresa.moneda %> <%= formatCurrency(saldo, 2) %></td>
                                                            <td>
                                                                <% if(p.estado == 'pendiente') { %>
                                                                    <span class="badge bg-warning text-dark px-2 rounded-pill">PENDIENTE</span>
                                                                <% } else if(p.estado == 'pagado') { %>
                                                                    <span class="badge bg-success px-2 rounded-pill">PAGADO</span>
                                                                <% } else { %>
                                                                    <span class="badge bg-danger px-2 rounded-pill">VENCIDO</span>
                                                                <% } %>
                                                            </td>
                                                            <td class="text-end pe-3">
                                                                <a href="/pagos/registrar/<%= p.id %>" class="btn btn-outline-success btn-sm"><i class="bi bi-cash"></i></a>
                                                                <a href="/prestamos/cronograma/<%= p.id %>" class="btn btn-outline-primary btn-sm"><i class="bi bi-calendar3"></i></a>
                                                            </td>
                                                        </tr>
                                                        <% }) %>
                                                    </tbody>
                                                </table>
                                            </div>
                                        <% } else { %>
                                            <div class="text-center py-5 text-muted">No hay historial de préstamos.</div>
                                        <% } %>
                                    </div>
                                </div>

                                <!-- Tab Empeños -->
                                <div class="tab-pane fade" id="empenos" role="tabpanel">
                                    <div class="card card-box p-3 border-0 shadow-sm">
                                        <% if(empenos.length > 0) { %>
                                            <div class="row row-cols-1 row-cols-md-2 g-3">
                                                <% empenos.forEach(e => { %>
                                                    <div class="col">
                                                        <div class="border rounded p-3 d-flex align-items-center bg-white shadow-sm">
                                                            <div class="bg-light rounded me-3 d-flex align-items-center justify-content-center border" style="width: 50px; height: 50px;">
                                                                <i class="bi bi-gem fs-4 text-primary"></i>
                                                            </div>
                                                            <div class="flex-grow-1">
                                                                <h6 class="mb-0 fw-bold"><%= e.nombre_articulo %></h6>
                                                                <small class="text-muted"><%= e.descripcion %></small>
                                                            </div>
                                                            <div class="text-end">
                                                                <span class="badge bg-primary d-block mb-1"><%= empresa.moneda %> <%= formatCurrency(e.monto_prestado, 2) %></span>
                                                                <span class="badge bg-info text-dark small"><%= e.estado %></span>
                                                            </div>
                                                        </div>
                                                    </div>
                                                <% }) %>
                                            </div>
                                        <% } else { %>
                                            <div class="text-center py-5 text-muted">No hay empeños registrados.</div>
                                        <% } %>
                                    </div>
                                </div>

                                <!-- Tab Ahorros -->
                                <div class="tab-pane fade" id="ahorros" role="tabpanel">
                                    <div class="card card-box p-4 text-center border-0 shadow-sm">
                                        <% if(cuentaAhorro) { %>
                                            <h6 class="text-muted text-uppercase mb-1">Saldo de Ahorros</h6>
                                            <h2 class="text-success fw-bold mb-3"><%= empresa.moneda %> <%= formatCurrency(cuentaAhorro.saldo_actual, 2) %></h2>
                                            <a href="/ahorros/ver/<%= cuentaAhorro.id %>" class="btn btn-primary px-4 btn-sm">Ver Movimientos</a>
                                        <% } else { %>
                                            <p class="text-muted">Sin cuenta de ahorros activa.</p>
                                            <a href="/ahorros/aperturar" class="btn btn-outline-primary btn-sm">Aperturar Cuenta</a>
                                        <% } %>
                                    </div>
                                </div>

                                <!-- Tab Pre-aprobados (NUEVA PESTAÑA) -->
                                <div class="tab-pane fade" id="preaprobados" role="tabpanel">
                                    <div class="card card-box border-0 shadow-sm overflow-hidden">
                                        <div class="row g-0">
                                            <div class="col-md-4 bg-primary text-white d-flex flex-column align-items-center justify-content-center p-5">
                                                <i class="bi bi-star-fill display-1 mb-3 opacity-50"></i>
                                                <h4 class="fw-bold">Cupo Especial</h4>
                                                <p class="small text-center opacity-75">Créditos de aprobación instantánea para clientes premium.</p>
                                            </div>
                                            <div class="col-md-8 p-5">
                                                <div class="text-center mb-4">
                                                    <p class="text-muted small mb-1 text-uppercase fw-bold">Cupo Disponible Actualmente</p>
                                                    <h1 id="montoPreDisplay" class="fw-bold text-primary mb-0" style="font-size: 3.5rem;">
                                                        <%= empresa.moneda %> <%= formatCurrency(cliente.monto_preaprobado || 0, 0) %>
                                                    </h1>
                                                </div>

                                                <div class="row g-3">
                                                    <div class="col-md-6">
                                                        <button onclick="editarCupoPreaprobado(<%= cliente.id %>, '<%= cliente.monto_preaprobado %>')" 
                                                                class="btn btn-outline-primary w-100 fw-bold py-3 rounded-pill">
                                                            <i class="bi bi-pencil-square me-2"></i> AJUSTAR CUPO
                                                        </button>
                                                    </div>
                                                    <div class="col-md-6">
                                                        <% if (cliente.email) { %>
                                                            <button onclick="enviarCorreoPreaprobado(<%= cliente.id %>)" 
                                                                    id="btnSendPre" 
                                                                    class="btn btn-warning w-100 fw-bold py-3 rounded-pill shadow-sm"
                                                                    <%= cliente.monto_preaprobado > 0 ? '' : 'disabled' %>>
                                                                <i class="bi bi-envelope-check-fill me-2"></i> NOTIFICAR CLIENTE
                                                            </button>
                                                        <% } else { %>
                                                            <div class="alert alert-light border small text-muted mb-0 h-100 d-flex align-items-center justify-content-center">
                                                                <i class="bi bi-info-circle me-2"></i> Registra un email para notificar.
                                                            </div>
                                                        <% } %>
                                                    </div>
                                                </div>

                                                <hr class="my-4">
                                                
                                                <div class="d-flex align-items-start gap-3">
                                                    <div class="bg-light p-3 rounded-circle">
                                                        <i class="bi bi-lightning-charge-fill text-warning fs-4"></i>
                                                    </div>
                                                    <div>
                                                        <h6 class="fw-bold mb-1">Beneficio de Pre-aprobación</h6>
                                                        <p class="small text-muted mb-0">Al asignar un cupo, el sistema habilita al cliente para solicitar desembolsos rápidos sin necesidad de re-evaluar documentos.</p>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Tab Documentos (NUEVA PESTAÑA) -->
                                <div class="tab-pane fade" id="documentos" role="tabpanel">
                                    <div class="card card-box border-0 shadow-sm p-4">
                                        <div class="row g-4">
                                            <!-- Listado de Documentos -->
                                            <div class="col-md-7">
                                                <h5 class="fw-bold mb-3"><i class="bi bi-folder-fill text-warning me-2"></i> Expediente Digital</h5>
                                                
                                                <% if (documentos && documentos.length > 0) { %>
                                                    <div class="table-responsive">
                                                        <table class="table table-hover align-middle">
                                                            <thead class="table-light text-uppercase small fw-bold">
                                                                <tr>
                                                                    <th>Documento</th>
                                                                    <th>Origen</th>
                                                                    <th>Fecha</th>
                                                                    <th>Estado</th>
                                                                    <th class="text-end">Acciones</th>
                                                                </tr>
                                                            </thead>
                                                            <tbody>
                                                                <% documentos.forEach(doc => { %>
                                                                    <tr>
                                                                        <td>
                                                                            <div class="d-flex align-items-center gap-2">
                                                                                <i class="bi <%= doc.archivo_url.toLowerCase().endsWith('.pdf') ? 'bi-file-pdf text-danger' : 'bi-image text-primary' %> fs-4"></i>
                                                                                <div>
                                                                                    <span class="fw-bold d-block"><%= doc.nombre_documento %></span>
                                                                                    <a href="<%= doc.archivo_url %>" target="_blank" class="small text-decoration-none">Abrir archivo <i class="bi bi-box-arrow-up-right"></i></a>
                                                                                </div>
                                                                            </div>
                                                                        </td>
                                                                        <td>
                                                                            <span class="badge bg-light text-dark border">
                                                                                <%= doc.subido_por === 'cliente' ? 'Cliente' : 'Administrador' %>
                                                                            </span>
                                                                        </td>
                                                                        <td><span class="small text-muted"><%= new Date(doc.fecha_subida).toLocaleDateString() %></span></td>
                                                                        <td>
                                                                            <% if (doc.estado === 'pendiente') { %>
                                                                                <span class="badge bg-warning text-dark px-2 rounded-pill">PENDIENTE</span>
                                                                            <% } else if (doc.estado === 'aprobado') { %>
                                                                                <span class="badge bg-success px-2 rounded-pill animate-pulse">APROBADO</span>
                                                                            <% } else { %>
                                                                                <span class="badge bg-danger px-2 rounded-pill" title="Motivo: <%= doc.motivo_rechazo %>">RECHAZADO</span>
                                                                                <% if (doc.motivo_rechazo) { %>
                                                                                    <div class="small text-danger mt-1" style="font-size: 0.75rem; max-width: 150px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;"><%= doc.motivo_rechazo %></div>
                                                                                <% } %>
                                                                            <% } %>
                                                                        </td>
                                                                        <td class="text-end">
                                                                            <div class="d-flex gap-1 justify-content-end">
                                                                                <% if (doc.estado !== 'aprobado') { %>
                                                                                    <button onclick="aprobarDoc(<%= doc.id %>)" class="btn btn-sm btn-outline-success" title="Aprobar"><i class="bi bi-check-lg"></i></button>
                                                                                <% } %>
                                                                                <% if (doc.estado !== 'rechazado') { %>
                                                                                    <button onclick="rechazarDoc(<%= doc.id %>)" class="btn btn-sm btn-outline-danger" title="Rechazar"><i class="bi bi-x-lg"></i></button>
                                                                                <% } %>
                                                                                <button onclick="eliminarDoc(<%= doc.id %>)" class="btn btn-sm btn-outline-secondary" title="Eliminar"><i class="bi bi-trash"></i></button>
                                                                            </div>
                                                                        </td>
                                                                    </tr>
                                                                <% }) %>
                                                            </tbody>
                                                        </table>
                                                    </div>
                                                <% } else { %>
                                                    <div class="text-center py-5 border rounded-4 bg-light">
                                                        <i class="bi bi-folder2-open display-6 text-muted mb-2"></i>
                                                        <p class="text-muted small mb-0">No hay documentos en el expediente de este cliente.</p>
                                                    </div>
                                                <% } %>
                                            </div>

                                            <!-- Subir Documento desde Admin -->
                                            <div class="col-md-5 border-start">
                                                <h5 class="fw-bold mb-3"><i class="bi bi-cloud-arrow-up-fill text-primary me-2"></i> Añadir al Expediente</h5>
                                                <p class="text-muted small">Puedes cargar documentos digitalizados directamente al expediente del cliente. Estos se marcarán como aprobados automáticamente.</p>
                                                
                                                <form action="/admin/documentos/subir/<%= cliente.id %>" method="POST" enctype="multipart/form-data" class="bg-light p-3 rounded-4 border">
                                                    <div class="mb-3">
                                                        <label class="form-label fw-bold small text-muted">Tipo de Documento</label>
                                                        <select name="nombre_documento" class="form-select" required>
                                                            <option value="Cédula de Identidad">Cédula de Ciudadanía / Identidad</option>
                                                            <option value="Comprobante de Domicilio">Comprobante de Domicilio</option>
                                                            <option value="Certificación Laboral">Certificación Laboral</option>
                                                            <option value="Historial Crediticio">Historial Crediticio</option>
                                                            <option value="Otro Documento">Otro</option>
                                                        </select>
                                                    </div>
                                                    
                                                    <div class="mb-3">
                                                        <label class="form-label fw-bold small text-muted">Seleccionar Archivo (PDF/Imagen)</label>
                                                        <input type="file" name="documento" class="form-control" accept="application/pdf, image/*" required>
                                                    </div>
                                                    
                                                    <button type="submit" class="btn btn-primary w-100 fw-bold rounded-pill">
                                                        <i class="bi bi-upload me-2"></i> CARGAR Y APROBAR
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Tab Scoring -->
                                <div class="tab-pane fade" id="scoring" role="tabpanel">
                                    <div class="card card-box border-0 shadow-sm overflow-hidden">
                                        <div class="row g-0">
                                            <!-- Columna Gráfica circular -->
                                            <div class="col-md-4 bg-light d-flex flex-column align-items-center justify-content-center p-5 border-end">
                                                <div class="score-ring-container position-relative mb-3" style="width: 160px; height: 160px;">
                                                    <svg width="160" height="160" class="score-ring-svg">
                                                        <circle stroke="#e2e8f0" stroke-width="10" fill="transparent" r="68" cx="80" cy="80" />
                                                        <circle id="scoreProgressRingAdmin" stroke="<%= scoreData.circularColor %>" stroke-width="12" stroke-linecap="round" fill="transparent" r="68" cx="80" cy="80" style="stroke-dasharray: 427; stroke-dashoffset: <%= 427 - (427 * scoreData.score / 1000) %>; transform: rotate(-90deg); transform-origin: 50% 50%; transition: stroke-dashoffset 1.5s cubic-bezier(0.4, 0, 0.2, 1), stroke 1.5s ease;" />
                                                    </svg>
                                                    <!-- Contenedor del valor del score actual en Admin -->
                                                    <div class="position-absolute top-50 start-50 translate-middle text-center" id="scoreValueContainerAdmin" style="transition: all 0.3s ease; z-index: 1;">
                                                        <h2 class="fw-bold mb-0 text-dark fs-2" id="scoreTextAdmin"><%= scoreData.score %></h2>
                                                        <span class="text-muted small text-uppercase fw-semibold" style="font-size: 0.75rem; letter-spacing: 0.5px;">Puntos</span>
                                                    </div>
                                                    <!-- Contenedor del spinner de carga premium en Admin -->
                                                    <div class="position-absolute top-50 start-50 translate-middle text-center d-flex flex-column align-items-center justify-content-center" id="scoreLoaderContainerAdmin" style="opacity: 0; pointer-events: none; transition: all 0.3s ease; z-index: 2; transform: translate(-50%, -50%) scale(0.7);">
                                                        <div class="spinner-border text-success" role="status" style="width: 2.2rem; height: 2.2rem; border-width: 0.2em;">
                                                            <span class="visually-hidden">Calculando...</span>
                                                        </div>
                                                        <span class="text-success fw-bold mt-1" style="font-size: 0.6rem; letter-spacing: 0.5px; text-uppercase;">Calculando</span>
                                                    </div>
                                                </div>
                                                <div class="text-center d-flex align-items-center justify-content-center gap-2 mt-2">
                                                    <span class="badge <%= scoreData.badgeBg %> text-white px-3 py-1.5 rounded-pill shadow-sm" id="scoreNivelAdmin">
                                                        Score: <%= scoreData.nivel %> (<%= scoreData.categoria %>)
                                                    </span>
                                                    <button onclick="mostrarAyudaScore()" class="btn btn-sm btn-link text-muted p-0 d-inline-flex align-items-center justify-content-center" title="Ver clasificación de scores" style="text-decoration: none; font-size: 1.15rem; transition: color 0.2s ease;">
                                                        <i class="bi bi-info-circle-fill text-success opacity-75"></i>
                                                    </button>
                                                </div>
                                                <small class="text-muted d-block mt-2" id="scoreFechaAdmin">
                                                    Cálculo: <%= scoreData.fechaCalculo ? new Date(scoreData.fechaCalculo).toLocaleDateString() : 'N/A' %>
                                                </small>
                                            </div>
                                            
                                            <!-- Columna Detalles y Recálculo -->
                                            <div class="col-md-8 p-5">
                                                <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-2">
                                                    <div>
                                                        <h5 class="fw-bold mb-1"><i class="bi bi-shield-check-fill text-success me-2"></i> Evaluación de Riesgo Interno</h5>
                                                        <p class="small text-muted mb-0">Variables matemáticas basadas en el comportamiento histórico del cliente.</p>
                                                    </div>
                                                    <button onclick="recalcularScoreAdmin(<%= cliente.id %>)" 
                                                            id="btnRecalcularScoreAdmin" 
                                                            class="btn btn-outline-success px-4 fw-bold rounded-pill shadow-sm d-flex align-items-center gap-2">
                                                        <i class="bi bi-arrow-clockwise" id="iconRecalcularScoreAdmin"></i> Recalcular Score
                                                    </button>
                                                </div>
                                                
                                                <!-- Desglose de puntos -->
                                                <div class="table-responsive mb-4">
                                                    <table class="table table-bordered table-striped align-middle mb-0 small">
                                                        <thead class="table-light fw-bold text-dark">
                                                            <tr>
                                                                <th>Regla / Variable Evaluada</th>
                                                                <th class="text-center" style="width: 150px;">Puntaje Impacto</th>
                                                                <th class="text-center" style="width: 150px;">Detalle del Cliente</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <tr>
                                                                <td class="text-dark"><strong>Puntaje Base del Sistema</strong></td>
                                                                <td class="text-center fw-bold text-dark">500 pts</td>
                                                                <td class="text-center text-muted">Estándar</td>
                                                            </tr>
                                                            <tr>
                                                                <td class="text-dark">Préstamos pagados a tiempo (+100 pts c/u, máx 300)</td>
                                                                <td class="text-center fw-bold text-success" id="desglosePagosAdmin">+<%= scoreData.desglose.pagadosATiempo %> pts</td>
                                                                <td class="text-center text-muted" id="detallePagosAdmin"><%= scoreData.detalles.prestamosPagadosATiempoCount %> préstamos</td>
                                                            </tr>
                                                            <tr>
                                                                <td class="text-dark">Saldo en Cuenta de Ahorro (+10 pts por cada $100.000, máx 100)</td>
                                                                <td class="text-center fw-bold text-success" id="desgloseAhorrosAdmin">+<%= scoreData.desglose.ahorros %> pts</td>
                                                                <td class="text-center text-muted" id="detalleAhorrosAdmin"><%= empresa.moneda %> <%= formatCurrency(scoreData.detalles.ahorroSaldo, 0) %></td>
                                                            </tr>
                                                            <tr>
                                                                <td class="text-dark">Antigüedad de cuenta (> 6 meses otorga +50)</td>
                                                                <td class="text-center fw-bold text-success" id="desgloseAntiguedadAdmin">+<%= scoreData.desglose.antiguedad %> pts</td>
                                                                <td class="text-center text-muted" id="detalleAntiguedadAdmin"><%= scoreData.detalles.antiguedadMeses %> meses</td>
                                                            </tr>
                                                            <tr>
                                                                <td class="text-dark">Préstamos activos en mora/vencidos (-200 pts c/u)</td>
                                                                <td class="text-center fw-bold <%= scoreData.desglose.prestamosVencidos < 0 ? 'text-danger' : 'text-muted' %>" id="desgloseVencidosAdmin"><%= scoreData.desglose.prestamosVencidos %> pts</td>
                                                                <td class="text-center text-muted" id="detalleVencidosAdmin"><%= scoreData.detalles.prestamosVencidosCount %> préstamos</td>
                                                            </tr>
                                                            <tr>
                                                                <td class="text-dark">Cuotas activas vencidas (-50 cuota < 30 días, -100 cuota > 30 días)</td>
                                                                <td class="text-center fw-bold <%= scoreData.desglose.cuotasVencidas < 0 ? 'text-danger' : 'text-muted' %>" id="desgloseCuotasAdmin"><%= scoreData.desglose.cuotasVencidas %> pts</td>
                                                                <td class="text-center text-muted" id="detalleCuotasAdmin"><%= scoreData.detalles.cuotasAtrasadasCount %> (< 30d) / <%= scoreData.detalles.cuotasAtrasadas30Count %> (> 30d)</td>
                                                            </tr>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                
                                                <!-- Consejos de Mejora -->
                                                <div class="bg-light p-3 rounded-4 border">
                                                    <h6 class="fw-bold text-dark small mb-2"><i class="bi bi-lightbulb-fill text-warning me-1"></i> Consejos dinámicos de salud crediticia:</h6>
                                                    <ul class="list-unstyled small text-muted mb-0" id="scoreConsejosListAdmin">
                                                        <% scoreData.consejos.forEach((consejo) => { %>
                                                            <li class="mb-1 d-flex align-items-start">
                                                                <i class="bi bi-chevron-right text-success small me-2 mt-1" style="font-size: 0.75rem;"></i>
                                                                <span><%= consejo %></span>
                                                            </li>
                                                        <% }) %>
                                                    </ul>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>
            </div>
        </div>
        <%- include('../partials/footer') %>
        <script>
            function enviarCorreoPreaprobado(clienteId) {
                const btn = document.getElementById('btnSendPre');
                const originalHtml = btn.innerHTML;
                
                Swal.fire({
                    title: '¿Enviar Notificación?',
                    text: "¿Deseas enviar un correo al cliente informándole sobre su cupo pre-aprobado?",
                    icon: 'question',
                    showCancelButton: true,
                    confirmButtonColor: '#3b82f6',
                    cancelButtonColor: '#6c757d',
                    confirmButtonText: 'Sí, enviar ahora',
                    cancelButtonText: 'Cancelar'
                }).then((result) => {
                    if (result.isConfirmed) {
                        btn.disabled = true;
                        btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span> Enviando...';

                        fetch(`/clientes/enviar-preaprobado/${clienteId}`)
                            .then(res => res.json())
                            .then(data => {
                                if (data.success) {
                                    Swal.fire('¡Enviado!', 'La notificación ha sido enviada correctamente.', 'success');
                                } else {
                                    Swal.fire('Error', data.mensaje || 'No se pudo enviar el correo.', 'error');
                                }
                            })
                            .catch(err => {
                                console.error(err);
                                Swal.fire('Error', 'Ocurrió un error en la conexión.', 'error');
                            })
                            .finally(() => {
                                btn.disabled = false;
                                btn.innerHTML = originalHtml;
                            });
                    }
                });
            }

            function iniciarChat(clienteId) {
                Swal.fire({
                    title: 'Enviar Mensaje Directo',
                    input: 'textarea',
                    inputLabel: 'Escribe el mensaje para iniciar o continuar la conversación',
                    inputPlaceholder: 'Hola, te escribimos para...',
                    showCancelButton: true,
                    confirmButtonText: 'Enviar Mensaje',
                    cancelButtonText: 'Cancelar',
                    confirmButtonColor: '#10b981',
                    inputValidator: (value) => {
                        if (!value || value.trim().length === 0) {
                            return '¡Debes escribir un mensaje!';
                        }
                    }
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                            title: 'Enviando...',
                            allowOutsideClick: false,
                            didOpen: () => {
                                Swal.showLoading();
                            }
                        });
                        fetch(`/admin/soporte/chat/${clienteId}/responder`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ mensaje: result.value })
                        })
                        .then(res => res.json())
                        .then(data => {
                            if (data.success) {
                                Swal.fire('¡Enviado!', 'El mensaje fue enviado correctamente. Redirigiendo al chat...', 'success').then(() => {
                                    window.location.href = '/admin/soporte'; // Redirigir a soporte
                                });
                            } else {
                                Swal.fire('Error', data.error || 'No se pudo enviar el mensaje.', 'error');
                            }
                        })
                        .catch(err => {
                            console.error(err);
                            Swal.fire('Error', 'Ocurrió un error en la conexión.', 'error');
                        });
                    }
                });
            }

            function editarCupoPreaprobado(clienteId, montoActual) {
                Swal.fire({
                    title: 'Ajustar Cupo Pre-aprobado',
                    input: 'text',
                    inputLabel: 'Ingrese el nuevo monto disponible',
                    inputValue: montoActual,
                    placeholder: '0',
                    showCancelButton: true,
                    confirmButtonText: 'Guardar Cambios',
                    cancelButtonText: 'Cancelar',
                    didOpen: () => {
                        const input = Swal.getInput();
                        // Aplicar máscara de moneda al input del modal
                        if (window.applyCurrencyMask) {
                            input.classList.add('money-mask');
                            applyCurrencyMask('.money-mask');
                        }
                    },
                    inputValidator: (value) => {
                        if (!value && value !== '0') {
                            return '¡Debes ingresar un monto!';
                        }
                    }
                }).then((result) => {
                    if (result.isConfirmed) {
                        fetch(`/clientes/actualizar-preaprobado/${clienteId}`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ monto: result.value })
                        })
                        .then(res => res.json())
                        .then(data => {
                            if (data.success) {
                                Swal.fire('¡Actualizado!', 'El cupo ha sido actualizado.', 'success').then(() => {
                                    location.reload(); // Recargar para ver los cambios
                                });
                            } else {
                                Swal.fire('Error', data.mensaje, 'error');
                            }
                        });
                    }
                });
            }

            // --- GESTIÓN DE DOCUMENTOS ---
            function aprobarDoc(docId) {
                Swal.fire({
                    title: '¿Aprobar Documento?',
                    text: "El cliente será notificado de la aprobación de su documento.",
                    icon: 'question',
                    showCancelButton: true,
                    confirmButtonColor: '#10b981',
                    cancelButtonColor: '#6c757d',
                    confirmButtonText: 'Sí, aprobar ahora',
                    cancelButtonText: 'Cancelar'
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                            title: 'Procesando...',
                            allowOutsideClick: false,
                            didOpen: () => { Swal.showLoading(); }
                        });
                        fetch(`/admin/documentos/estado/${docId}`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ estado: 'aprobado' })
                        })
                        .then(res => res.json())
                        .then(data => {
                            if (data.success) {
                                Swal.fire('¡Aprobado!', 'El documento ha sido aprobado.', 'success').then(() => {
                                    location.reload();
                                });
                            } else {
                                Swal.fire('Error', data.error, 'error');
                            }
                        })
                        .catch(err => {
                            console.error(err);
                            Swal.fire('Error', 'Conexión fallida.', 'error');
                        });
                    }
                });
            }

            function rechazarDoc(docId) {
                Swal.fire({
                    title: '¿Rechazar Documento?',
                    input: 'textarea',
                    inputLabel: 'Ingresa el motivo del rechazo para notificar al cliente',
                    inputPlaceholder: 'Ej: El documento está borroso o incompleto...',
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#ef4444',
                    cancelButtonColor: '#6c757d',
                    confirmButtonText: 'Rechazar',
                    cancelButtonText: 'Cancelar',
                    inputValidator: (value) => {
                        if (!value || value.trim().length === 0) {
                            return '¡Debes ingresar un motivo para el rechazo!';
                        }
                    }
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                            title: 'Procesando...',
                            allowOutsideClick: false,
                            didOpen: () => { Swal.showLoading(); }
                        });
                        fetch(`/admin/documentos/estado/${docId}`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ estado: 'rechazado', motivo_rechazo: result.value })
                        })
                        .then(res => res.json())
                        .then(data => {
                            if (data.success) {
                                Swal.fire('Rechazado', 'El documento ha sido rechazado y el cliente notificado.', 'success').then(() => {
                                    location.reload();
                                });
                            } else {
                                Swal.fire('Error', data.error, 'error');
                            }
                        })
                        .catch(err => {
                            console.error(err);
                            Swal.fire('Error', 'Conexión fallida.', 'error');
                        });
                    }
                });
            }

            function eliminarDoc(docId) {
                Swal.fire({
                    title: '¿Eliminar Documento?',
                    text: "Esta acción no se puede deshacer. Se borrará el archivo de forma definitiva.",
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#ef4444',
                    cancelButtonColor: '#6c757d',
                    confirmButtonText: 'Sí, eliminar',
                    cancelButtonText: 'Cancelar'
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                            title: 'Eliminando...',
                            allowOutsideClick: false,
                            didOpen: () => { Swal.showLoading(); }
                        });
                        fetch(`/admin/documentos/eliminar/${docId}`, {
                            method: 'DELETE'
                        })
                        .then(res => res.json())
                        .then(data => {
                            if (data.success) {
                                Swal.fire('¡Eliminado!', 'El documento ha sido borrado.', 'success').then(() => {
                                    location.reload();
                                });
                            } else {
                                Swal.fire('Error', data.error, 'error');
                            }
                        })
                        .catch(err => {
                            console.error(err);
                            Swal.fire('Error', 'Conexión fallida.', 'error');
                        });
                    }
                });
            }

            async function recalcularScoreAdmin(clienteId) {
                const btn = document.getElementById('btnRecalcularScoreAdmin');
                const icon = document.getElementById('iconRecalcularScoreAdmin');
                const ring = document.getElementById('scoreProgressRingAdmin');
                const scoreEl = document.getElementById('scoreTextAdmin');
                const valContainer = document.getElementById('scoreValueContainerAdmin');
                const loaderContainer = document.getElementById('scoreLoaderContainerAdmin');
                
                if (!btn || btn.disabled) return;

                btn.disabled = true;
                icon.style.animation = 'spin 1s linear infinite';

                // Guardar valores originales
                const originalOffset = ring.style.strokeDashoffset;
                const originalColor = ring.getAttribute('stroke');

                // Agregar estilo spin si no existe
                if (!document.getElementById('spinStyleAdmin')) {
                    const style = document.createElement('style');
                    style.id = 'spinStyleAdmin';
                    style.innerHTML = `
                        @keyframes spin {
                            0% { transform: rotate(0deg); }
                            100% { transform: rotate(360deg); }
                        }
                    `;
                    document.head.appendChild(style);
                }

                // 1. Ocultar el valor actual con transición de escala y opacidad
                valContainer.style.opacity = '0';
                valContainer.style.transform = 'translate(-50%, -50%) scale(0.7)';

                // 2. Vaciar el círculo de progreso de forma fluida
                ring.style.transition = 'stroke-dashoffset 0.5s cubic-bezier(0.4, 0, 0.2, 1)';
                ring.style.strokeDashoffset = '427';

                // 3. Mostrar el loader central premium
                setTimeout(() => {
                    if (btn.disabled) {
                        loaderContainer.style.opacity = '1';
                        loaderContainer.style.transform = 'translate(-50%, -50%) scale(1)';
                    }
                }, 150);

                try {
                    const response = await fetch(`/clientes/recalcular-score/${clienteId}`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' }
                    });

                    const data = await response.json();

                    // Mínimo de tiempo de carga para apreciar la animación premium y fluida
                    await new Promise(resolve => setTimeout(resolve, 1200));

                    if (data.success) {
                        const sd = data.scoreData;

                        // 1. Ocultar el loader central
                        loaderContainer.style.opacity = '0';
                        loaderContainer.style.transform = 'translate(-50%, -50%) scale(0.7)';

                        // Esperar a que se oculte el loader antes de mostrar el nuevo valor
                        await new Promise(resolve => setTimeout(resolve, 200));

                        // 2. Preparar y mostrar el valor definitivo desde escala reducida
                        scoreEl.textContent = '0';
                        valContainer.style.transform = 'translate(-50%, -50%) scale(0.7)';
                        valContainer.getBoundingClientRect(); // Reflow
                        valContainer.style.opacity = '1';
                        valContainer.style.transform = 'translate(-50%, -50%) scale(1)';

                        // 3. Rellenar el círculo al nuevo valor con efecto elástico premium
                        ring.style.transition = 'stroke-dashoffset 1.8s cubic-bezier(0.34, 1.56, 0.64, 1), stroke 1s ease';
                        ring.setAttribute('stroke', sd.circularColor);
                        const offset = 427 - (427 * sd.score / 1000);
                        ring.style.strokeDashoffset = offset;

                        // 4. Animación interactiva de Conteo de Puntos
                        const start = 0;
                        const end = sd.score;
                        const duration = 1500;
                        const startTime = performance.now();

                        function animate(now) {
                            const elapsed = now - startTime;
                            const progress = Math.min(elapsed / duration, 1);
                            const easeProgress = 1 - Math.pow(1 - progress, 3); // Cubic Out
                            const current = Math.floor(start + easeProgress * (end - start));
                            scoreEl.textContent = current;
                            if (progress < 1) {
                                requestAnimationFrame(animate);
                            } else {
                                scoreEl.textContent = end;
                            }
                        }
                        requestAnimationFrame(animate);

                        // Actualizar nivel y fecha
                        document.getElementById('scoreNivelAdmin').className = `badge ${sd.badgeBg} text-white px-3 py-1.5 rounded-pill shadow-sm`;
                        document.getElementById('scoreNivelAdmin').textContent = `Score: ${sd.nivel} (${sd.categoria})`;
                        document.getElementById('scoreFechaAdmin').textContent = `Cálculo: ${new Date(sd.fechaCalculo).toLocaleDateString()}`;

                        // Actualizar Desglose
                        document.getElementById('desglosePagosAdmin').textContent = `+${sd.desglose.pagadosATiempo} pts`;
                        document.getElementById('desgloseAhorrosAdmin').textContent = `+${sd.desglose.ahorros} pts`;
                        document.getElementById('desgloseAntiguedadAdmin').textContent = `+${sd.desglose.antiguedad} pts`;

                        const dVencidos = document.getElementById('desgloseVencidosAdmin');
                        dVencidos.textContent = `${sd.desglose.prestamosVencidos} pts`;
                        dVencidos.className = `text-center fw-bold ${sd.desglose.prestamosVencidos < 0 ? 'text-danger' : 'text-muted'}`;

                        const dCuotas = document.getElementById('desgloseCuotasAdmin');
                        dCuotas.textContent = `${sd.desglose.cuotasVencidas} pts`;
                        dCuotas.className = `text-center fw-bold ${sd.desglose.cuotasVencidas < 0 ? 'text-danger' : 'text-muted'}`;

                        // Actualizar Detalle
                        document.getElementById('detallePagosAdmin').textContent = `${sd.detalles.prestamosPagadosATiempoCount} préstamos`;
                        document.getElementById('detalleAhorrosAdmin').textContent = `$${sd.detalles.ahorroSaldo.toLocaleString('es-CO')}`;
                        document.getElementById('detalleAntiguedadAdmin').textContent = `${sd.detalles.antiguedadMeses} meses`;
                        document.getElementById('detalleVencidosAdmin').textContent = `${sd.detalles.prestamosVencidosCount} préstamos`;
                        document.getElementById('detalleCuotasAdmin').textContent = `${sd.detalles.cuotasAtrasadasCount} (< 30d) / ${sd.detalles.cuotasAtrasadas30Count} (> 30d)`;

                        // Actualizar Consejos
                        const list = document.getElementById('scoreConsejosListAdmin');
                        list.innerHTML = '';
                        sd.consejos.forEach(consejo => {
                            const li = document.createElement('li');
                            li.className = 'mb-1 d-flex align-items-start';
                            li.innerHTML = `<i class="bi bi-chevron-right text-success small me-2 mt-1" style="font-size: 0.75rem;"></i><span>${consejo}</span>`;
                            list.appendChild(li);
                        });

                        // Alerta SweetAlert2
                        Swal.fire({
                            icon: 'success',
                            title: '¡Score Recalculado!',
                            text: `El nuevo score crediticio del cliente es de ${sd.score} puntos (Categoría ${sd.categoria}).`,
                            confirmButtonColor: '#10b981'
                        });

                    } else {
                        // Restaurar
                        loaderContainer.style.opacity = '0';
                        loaderContainer.style.transform = 'translate(-50%, -50%) scale(0.7)';
                        valContainer.style.opacity = '1';
                        valContainer.style.transform = 'translate(-50%, -50%) scale(1)';
                        
                        ring.style.transition = 'stroke-dashoffset 0.8s ease';
                        ring.style.strokeDashoffset = originalOffset;
                        ring.setAttribute('stroke', originalColor);
                        
                        Swal.fire('Error', data.mensaje || 'Error al recalcular el score.', 'error');
                    }
                } catch (err) {
                    console.error("Error al recalcular score:", err);
                    // Restaurar
                    loaderContainer.style.opacity = '0';
                    loaderContainer.style.transform = 'translate(-50%, -50%) scale(0.7)';
                    valContainer.style.opacity = '1';
                    valContainer.style.transform = 'translate(-50%, -50%) scale(1)';
                    
                    ring.style.transition = 'stroke-dashoffset 0.8s ease';
                    ring.style.strokeDashoffset = originalOffset;
                    ring.setAttribute('stroke', originalColor);
                    
                    Swal.fire('Error de Red', 'No se pudo conectar con el servidor.', 'error');
                } finally {
                    btn.disabled = false;
                    icon.style.animation = '';
                }
            }

            function mostrarAyudaScore() {
                Swal.fire({
                    title: 'Clasificación del Score Crediticio',
                    html: `
                        <div class="table-responsive text-start small mt-2">
                            <table class="table table-sm table-bordered align-middle mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th>Categoría</th>
                                        <th>Rango Puntos</th>
                                        <th>Nivel de Riesgo</th>
                                        <th>Tasa (Int. / Mora)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr style="background-color: rgba(16, 185, 129, 0.08);">
                                        <td><span class="badge bg-success px-2 py-1">Categoría A</span></td>
                                        <td class="fw-bold text-success">900 - 1000 pts</td>
                                        <td class="fw-bold text-success">Excelente</td>
                                        <td class="fw-bold text-success">1.5% / 2.5%</td>
                                    </tr>
                                    <tr style="background-color: rgba(59, 130, 246, 0.08);">
                                        <td><span class="badge bg-info px-2 py-1">Categoría B</span></td>
                                        <td class="fw-bold text-primary">700 - 899 pts</td>
                                        <td class="fw-bold text-primary">Bueno</td>
                                        <td class="fw-bold text-primary">2.0% / 3.0%</td>
                                    </tr>
                                    <tr style="background-color: rgba(245, 158, 11, 0.08);">
                                        <td><span class="badge bg-warning text-dark px-2 py-1">Categoría C</span></td>
                                        <td class="fw-bold text-warning">500 - 699 pts</td>
                                        <td class="fw-bold text-warning">Regular</td>
                                        <td class="fw-bold text-warning">2.5% / 3.5%</td>
                                    </tr>
                                    <tr style="background-color: rgba(248, 113, 113, 0.08);">
                                        <td><span class="badge px-2 py-1 text-white" style="background-color: #f87171;">Categoría D</span></td>
                                        <td class="fw-bold text-danger">300 - 499 pts</td>
                                        <td class="fw-bold text-danger">Malo</td>
                                        <td class="fw-bold text-danger">3.0% / 4.0%</td>
                                    </tr>
                                    <tr style="background-color: rgba(220, 38, 38, 0.08);">
                                        <td><span class="badge bg-danger px-2 py-1">Categoría E</span></td>
                                        <td class="fw-bold text-danger" style="color: #dc2626 !important;">0 - 299 pts</td>
                                        <td class="fw-bold text-danger" style="color: #dc2626 !important;">Crítico</td>
                                        <td class="fw-bold text-danger" style="color: #dc2626 !important;">3.5% / 4.5%</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <p class="small text-muted text-start mt-3 mb-0">
                            <i class="bi bi-info-circle text-success me-1"></i>
                            El puntaje base inicia en <strong>500 puntos</strong>. Sube pagando a tiempo (+100 c/u, máx 300), manteniendo saldo en cuenta de ahorros (+10 por cada $100.000, máx 100) y por antigüedad (+50). Baja por cuotas en mora (-50/-100) y créditos vencidos (-200 c/u).
                        </p>
                    `,
                    confirmButtonText: 'Entendido',
                    confirmButtonColor: '#10b981',
                    customClass: {
                        popup: 'rounded-4'
                    }
                });
            }
        </script>
    </body>
</html>